// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://seedwithroot.github.io',
  base: '/landingpages-ourbond',
  outDir: 'docs',
});
